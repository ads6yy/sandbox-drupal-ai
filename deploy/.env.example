COMPOSE_PROJECT_NAME=drupal-ai
DB_ROOT_PASSWORD=change_me_root
DB_NAME=drupal-ai
DB_USER=drupal-ai
DB_PASSWORD=change_me
DB_HOST=mariadb

# Sel de hachage Drupal (sessions, tokens) — une valeur aléatoire par environnement
HASH_SALT=change_me_random_string

# Clé API Google Gemini (provider AI) — ne jamais committer la vraie valeur
GEMINI_API_KEY=change_me

# Langfuse Cloud (observabilité LLM) — projet sur cloud.langfuse.com, région EU
LANGFUSE_PUBLIC_KEY=pk-lf-change_me
LANGFUSE_SECRET_KEY=sk-lf-change_me
LANGFUSE_BASE_URL=https://cloud.langfuse.com
